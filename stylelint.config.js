module.exports = {
	extends: ['stylelint-config-standard', 'stylelint-config-prettier'],
	rules: {
		'prettier/prettier': true,
		'declaration-block-single-line-max-declarations': null // Désactive la règle
	}
}
